name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Show info
      run: |
        echo ${GITHUB_REF:11}
    - uses: actions/upload-artifact@v1
      with:
        name: my-artifact
        path: src/
